# Random baseline agent

def random_agent(bitboard, legal_moves, rng=None):
    """
    Choose a legal move uniformly at random.
    """
    rng = rng or random
    return rng.choice(legal_moves)


RANDOM_AGENT_NAME = "random"
RANDOM_AGENT_GAMES = 100

random_artifact_100 = run_experiment(
    name=RANDOM_AGENT_NAME,
    agent_fn=random_agent,
    n_games=RANDOM_AGENT_GAMES,
    base_seed=BASE_SEED,
    save_replays=True,
    force=False,
)

random_artifact_100["summary"]